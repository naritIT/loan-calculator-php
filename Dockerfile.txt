# ใช้ PHP เวอร์ชันล่าสุดที่มี Apache ในตัว
FROM php:8.2-apache

# คัดลอกไฟล์ทั้งหมดในโปรเจกต์ไปที่โฟลเดอร์เว็บใน container
COPY . /var/www/html/

# เปิดพอร์ต 80 สำหรับเว็บ
EXPOSE 80

# สั่งให้ Apache รัน
CMD ["apache2-foreground"]
